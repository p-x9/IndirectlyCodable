import XCTest
@testable import IndirectlyCodable

final class IndirectlyCodableTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(IndirectlyCodable().text, "Hello, World!")
    }
}
